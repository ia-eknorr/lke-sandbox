clusterSecretStore:
  # Use internal URL - public URL requires Traefik + cert-manager which deploy AFTER external-secrets
  infisicalUrl: http://infisical-upstream.infisical.svc:8080
  secretsScope:
    projectSlug: "sandbox"
    environmentSlug: "dev"
